# v0.1.0 internal fake-first/local baseline

This release promotes the current `develop` baseline to `main` as an internal fake-first/local baseline.

It is intended for repository review, local Docker/runtime rehearsal, contract validation, and follow-up release hardening. It does **not** claim production readiness, public Discord readiness, live Discord execution, live social publishing/scheduling, live analytics ingestion, or durable production writes.

## Release scope

Status statement preserved for this baseline:

> Internal fake-first/local baseline complete. Private Docker and local disposable validations passed. QA-07 private Discord routing execution remains gated and was not executed. Production readiness is not claimed.

The release owner explicitly defers private Discord execution for this tag. QA-07/private Discord execution remains gated until a separate approved issue executes and reviews `docs/operations/private-discord-manual-verification-guide.md` with sanitized evidence.

## Promoted changes since `main`

| PR | Summary |
| --- | --- |
| #164 | Align issue/PR templates with the develop-to-main release workflow. |
| #165 | Harden stable merge checks and repository validation gates. |
| #167 | Package Gentle-AI into the local OpenClaw runtime image. |
| #169 | Package the Engram CLI for OpenClaw MCP support. |
| #171 | Document OpenAI OAuth setup and runtime verification steps. |
| #172 | Add OpenClaw Global channel guides and validation contracts. |
| #173 | Add the safe fake-first `/project delete` contract. |
| #178 | Align CI workflow trigger documentation with `develop` and `main`. |
| #179 | Record local OpenClaw + Gentle orchestration validation evidence. |
| #180 | Curate the OpenClaw skill surface around runtime-core and scoped workflow skills. |

## Validation evidence

Local safe validation used for this release promotion:

```bash
git diff --check
bash scripts/validate-repo-safe-evidence.sh
bash scripts/validate-openclaw-gentle-ai-runtime.sh
bash scripts/validate-openclaw-skill-inventory.sh
SAFE_VALIDATION_SKIP_STAGE0=1 bash scripts/run-safe-validation-suite.sh
```

PR checks on the release-promotion PR must also pass before merge to `main`.

## Known limitations

- QA-07/private Discord execution is gated and was not executed for this baseline.
- No public Discord behavior is claimed.
- No production hosting, high availability, backup/restore, or operational SLO is claimed.
- No live social publishing, scheduling, Buffer activity, or live analytics ingestion is claimed.
- OpenClaw + Gentle orchestration validation is local/no-op and model-mediated; deterministic resolver hardening remains a future runtime concern.
- Gentle SDD assets are packaged as OpenClaw workspace protocol/backend assets, not Pi subagents running inside OpenClaw.

## Rollback notes

This promotion is repository-only. If a release problem is found after merge, open a hotfix issue/PR against `main`, then back-merge or cherry-pick the fix into `develop`.

## Post-merge actions

After the release-promotion PR merges:

1. Pull `main` locally.
2. Tag `main` as `v0.1.0` if the maintainer approves tagging.
3. Create a GitHub release using these notes.
4. Move promoted project items from `Merged to develop` to `Merged to main`.
